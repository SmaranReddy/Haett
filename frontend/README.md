# Frontend — Partner Portal

React 19 + Vite 8 + Tailwind v4 SPA.

See [root README](../README.md) for full setup instructions.

## Quick Start

```bash
npm install
npm run dev        # Starts on :5173, proxies /api to :3000
npm run build      # Production build to dist/
```
